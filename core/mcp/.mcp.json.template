{
  "_comment_purpose": "Suggested MCP client config. Generated as .mcp.json.suggested when --with-mcp-discovery is set. Review before promoting to .mcp.json.",
  "_comment_credentials": "Use env var references such as ${GITHUB_TOKEN}. Never inline real tokens here; scripts/lib/validate_mcp_config.py rejects inline secrets.",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
